import React from "react";
import { StyleSheet, View, Text } from "react-native";

const styles = StyleSheet.create({
  appStyles: { flex: 1 },
});

export const { appStyles } = styles;

export const options = {};

export { React, View, Text };
