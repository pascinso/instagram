import React from "react";
import { StyleSheet } from "react-native";
import { Feather } from "@expo/vector-icons";

const styles = StyleSheet.create({
  leftStyles: { marginLeft: 10 },
});

export const { leftStyles } = styles;

export { React, Feather };
